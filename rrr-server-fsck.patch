--- /usr/local/bin/rrr-server
+++ /cpan/content/rrr-server-2
@@ -187,7 +187,7 @@
 # Need also to verify that we watch all directories we encounter.
 sub fsck {
     my($rf) = @_;
-    0 == system $^X, "-Ilib", "bin/rrr-fsck", $rf->rfile, "--verbose" or die;
+    0 == system "rrr-fsck", $rf->rfile, "--verbose" or die;
 }
 
 MAIN: {
